install

```bash
poetry install
poetry build
```

Active shell

```
poetry shell
```

use pipx to install the cli commands.

```
pipx install dist/typecmd-0.1.0.tar.gz
```

Now you can use `typecmd`.
